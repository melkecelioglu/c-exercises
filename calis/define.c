#include <stdio.h>
#include <math.h>

#define kare(x) (x*x)
#define hipo(x,y) sqrt(x*x + y*y)

int main(){

int a,b=3;

   printf("kare(2) = %d\n", kare(2));

    printf("hipo(a,b)= %f\n", hipo(a,b));
    return 0;


}