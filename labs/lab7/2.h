#include <stdio.h>




typedef struct complex{
    float re;
    float im;
   
} complex;

complex topla(complex*, complex*);

complex carpim(complex*, complex*);
void yadirKarmasik(complex);
void yazdirKarmasikDizi(complex*, int);
